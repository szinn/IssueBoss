#shellcheck disable=SC2148,SC2155

use_sops() {
  local path=${1:-$PWD/secrets.yaml}
  eval "$(sops -d --output-type dotenv "$path" | direnv dotenv bash /dev/stdin)"
  watch_file "$path"
}

export CLIPPY_CONF_DIR=".config"
export RENOVATE_AUTODISCOVER="true"
export RENOVATE_AUTODISCOVER_FILTER="szinn/IssueBoss"
export RUST_LOG="TRACE"
export DOCKER_HOST="unix://$HOME/.config/colima/default/docker.sock"

export ISSUEBOSS__HTTP_PORT="8082"
export ISSUEBOSS_GRPC_HOST="http://odin"
export ISSUEBOSS_GRPC_PORT="9090"
export ISSUEBOSS_MCP_PORT="8090"

use_sops config.sops.env
